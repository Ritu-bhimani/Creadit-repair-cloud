import { Container } from '@mui/material';
import { Banner } from '../../components';

export const InvoicesPage = () => {
  return (
    <Container maxWidth="xl">
      <Banner pageTitle="Invoices (all clients)" />
    </Container>
  );
};
